name: 'banking_bq'
version: '1.0.0'
config-version: 2

profile: 'banking_bq'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  banking_bq:
    staging:
      +materialized: view
      +dataset: banking_staging
    marts:
      +materialized: table
      +dataset: banking_marts